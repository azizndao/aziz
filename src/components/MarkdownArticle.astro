---
interface Props {
    class?: string;
}
---

<article class:list={["markdown", Astro.props.class]}>
    <slot/>
</article>

<style is:global>
    * {
        scroll-padding-top: calc(var(--header-height) * 1.5);
    }

    .markdown {
        padding: 1rem;
    }

    .markdown h1 {
        font-size: 2.25rem;
        line-height: 2.5rem;
    }

    @media (min-width: 768px) {
        .markdown h1 {
            font-size: 3rem /* 48px */;
            line-height: 1;
        }
    }

    .markdown :is(h1, h2, h3, h4, h5, h6) {
        line-height: 1.2;
        margin-bottom: 1rem;
        font-weight: 600;
    }

    @media (min-width: 768px) {
        .markdown :is(h1, h2, h3, h4, h5, h6) {
            margin-top: 2rem;
        }
    }

    .markdown :is(h1, h2, h3, h4, h5, h6) a {
        color: var(--primary);
        text-decoration-line: none;
    }

    .markdown :is(h1, h2, h3, h4, h5, h6) a:after {
        content: "#";
        font-weight: lighter;
        padding-left: 0.75rem;
        padding-right: 0.75rem;
        opacity: 0;
    }

    .markdown :is(h1, h2, h3, h4, h5, h6):hover a:after {
        opacity: 1;
        color: rgba(var(--primary));
    }

    .markdown h2 {
        font-size: 1.875rem;
        line-height: 2.25rem;
    }

    @media (min-width: 768px) {
        .markdown h2 {
            font-size: 2.25rem;
            line-height: 2.5rem;
        }
    }

    .markdown h3 {
        font-size: 1.5rem;
        line-height: 2rem;
    }

    @media (min-width: 768px) {
        .markdown h3 {
            font-size: 1.875rem;
            line-height: 2.25rem;
        }
    }

    .markdown h4 {
        font-size: 1.25rem;
        line-height: 1.75rem;
    }

    @media (min-width: 768px) {
        .markdown h4 {
            font-size: 1.5rem;
            line-height: 2rem;
        }
    }

    .markdown h5 {
        font-size: 1.125rem;
        line-height: 1.75rem;
    }

    @media (min-width: 768px) {
        .markdown h5 {
            font-size: 1.25rem;
            line-height: 1.75rem;
        }
    }

    .markdown p {
        margin-top: 1.5rem;
        margin-bottom: 1.5rem;
        line-height: 1.75;
        font-size: 1rem;
    }

    @media (min-width: 1024px) {
        .markdown p {
            font-size: 1.125rem;
            line-height: 1.75rem;
        }
    }

    .markdown :is(ul, ol) {
        list-style-position: inside;
        padding-left: 1rem;
        padding-right: 1rem;
        padding-top: 0.5rem;
        padding-bottom: 0.5rem;
    }

    .markdown ul li {
        list-style-type: disc;
        margin-top: 1rem;
        margin-bottom: 1rem;
    }

    .markdown ol li {
        list-style-type: decimal;
        margin-top: 1rem;
        margin-bottom: 1rem;
    }

    .markdown :is(ul, ol) li::marker {
        font-weight: 800;
        color: rgb(var(--primary));
        list-style-position: inside;
    }

    .markdown a {
        color: rgb(var(--primary));
        text-decoration-color: rgb(var(--primary));
        text-decoration: 2px solid rgb(var(--primary));
    }

    .markdown code {
        font-family: var(--font-monospace);
        padding: 4px 6px;
        border-radius: 6px;
        background-color: var(--surface-variant);
        color: var(--on-surface-variant);
    }

    .markdown code::selection {
        background: transparent;
        border-radius: 0%;
    }

    .markdown pre {
        padding: 1rem;
        border-radius: 0.4rem;
        overflow-x: auto;
        margin: 0;
    }

    .markdown pre code {
        background-color: transparent !important;
        margin: 0;
        padding: 0;
        border: none;
        font-size: 0.875rem /* 14px */;
        line-height: 1.25rem;
        background-color: rgb(var(--surface-variant));
    }

    @media (min-width: 768px) {
        .markdown pre code {
            font-size: 1rem;
            line-height: 1.5rem;
        }
    }

    .markdown pre code .line {
        line-height: 1.8;
    }

    [data-rehype-pretty-code-fragment] {
        overflow: auto;
        color: white;
        background-color: rgb(1 31 51);
        border-radius: 8px;
        margin: 20px 0;
    }

    [data-rehype-pretty-code-fragment] pre {
        --pretty-code-fragment-max-height: 600px;
        overflow: auto;
        border-radius: 8px;
        max-height: var(--pretty-code-fragment-max-height);
    }

    [data-rehype-pretty-code-title] {
        padding: 5px 15px;
        font-family: monospace;
        font-weight: bold;
        background-color: rgb(var(--primary));
    }

    .markdown .text-gradient {
        --accent-gradient: linear-gradient(
                45deg,
                #f09433 0%,
                #e6683c 25%,
                #dc2743 50%,
                #cc2366 75%,
                #bc1888 100%
        );
        background-image: var(--accent-gradient);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-size: 400%;
        background-position: 0%;
    }

    .markdown .instructions {
        line-height: 1.6;
        margin: 1rem 0;
        border: 1px solid rgba(var(--accent), 25%);
        background-color: white;
        padding: 1rem;
        border-radius: 0.4rem;
    }

    .markdown table {
        border-collapse: collapse;
        table-layout: auto;
        width: 100%;
        font-size: 0.875rem /* 14px */;
        line-height: 1.25rem;
    }

    .markdown table th {
        border-bottom-width: 1px;
        border-bottom-style: solid;
        border-bottom-color: rgb(var(----outline));
        font-weight: 500;
        padding: 1rem;
        padding-left: 2rem;
        padding-top: 0px;
        padding-bottom: 0.75rem /* 12px */;
        color: rgb(var(--on-surface-variant));
        text-align: left;
    }

    .markdown table tbody {
        background-color: rgb(var(--surface));
    }

    .markdown table td {
        border-color: rgb(var(--outline));
        border-bottom-width: 1px;
        padding: 1rem;
        padding-left: 2rem;
        color: rgb(var(--on-surface-variant));
    }

    .markdown blockquote {
        padding: 20px 13px 20px 10px;
        margin: 30px 0;
        border-left-width: 10px;
        background-color: rgb(var(--primary-container));
        border-color: rgb(var(--primary));
    }

    .markdown blockquote p:first-child {
        margin-top: 0;
    }

    .markdown blockquote p:last-child {
        margin-bottom: 0;
    }

    .markdown img {
        border-radius: 5px;
        overflow: hidden;
        max-width: 100%;
        object-fit: cover;
    }
</style>
