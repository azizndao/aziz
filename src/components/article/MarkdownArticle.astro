---
import { twMerge } from "tailwind-merge";
interface Props {
  class?: string;
}
---

<article
  class={twMerge(
    "markdown prose prose-neutral dark:prose-invert lg:prose-lg xl:prose-xl",
    Astro.props.class
  )}
>
  <slot />
</article>
