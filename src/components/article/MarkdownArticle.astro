---
import { twMerge } from 'tailwind-merge'
interface Props {
  class?: string
}
---

<article
  class={twMerge(
    'markdown mx-auto prose prose-slate dark:prose-invert lg:prose-lg !prose-pre:bg-neutral-300',
    Astro.props.class
  )}
>
  <slot />
</article>
